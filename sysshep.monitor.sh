# sysshep.monitor.sh: ordinary ublu monitoring 
# autogenerated Thu Jan 05 17:15:57 MST 2017 by taylor using command:
# gensh -to sysshep.monitor.sh -path /opt/ublu/ublu.jar -includepath $SCRIPTDIR -optr p PROPERTIES @properties ${ credentials properties file }$ ${ sysshep.monitor.sh: ordinary ublu monitoring }$ sysshep/monitor.ublu ${ sysshep.monitor ( @properties ) }$

# Usage message
function usage {
echo "sysshep.monitor.sh: ordinary ublu monitoring "
echo "This shell script was autogenerated Thu Jan 05 17:15:57 MST 2017 by taylor."
echo "Usage: $0 [silent] -h -p PROPERTIES "
echo "	where"
echo "	-h		display this help message and exit 0"
echo "	-p PROPERTIES	credentials properties file  (required option)"
echo "---"
echo "If the keyword 'silent' appears ahead of all options, then included files will not echo and prompting is suppressed."
echo "Exit code is the result of execution, or 0 for -h or 2 if there is an error in processing options"
}

#Test if user wants silent includes
if [ "$1" == "silent" ]
then
	SILENT="-silent "
	shift
else
	SILENT=""
fi

# Process options
while getopts p:h the_opt
do
	case "$the_opt" in
		p)	PROPERTIES="$OPTARG";;
		h)	usage;exit 0;;
		[?])	usage;exit 2;;

	esac
done
shift `expr ${OPTIND} - 1`
if [ $# -ne 0 ]
then
	echo "Superfluous argument(s) $*"
	usage
	exit 2
fi

# Translate options to tuple assignments
if [ "${PROPERTIES}" != "" ]
then
	gensh_runtime_opts="${gensh_runtime_opts}string -to @properties -trim \${ ${PROPERTIES} }$ "
else
	echo "Option -p PROPERTIES is a required option but is not present."
	usage
	exit 2
fi

SCRIPTDIR=$(CDPATH= cd "$(dirname "$0")" && pwd)

# Invocation
java -Dublu.includepath="$SCRIPTDIR" -jar /opt/ublu/ublu.jar ${gensh_runtime_opts} include ${SILENT}sysshep/monitor.ublu sysshep.monitor \( @properties \) 
exit $?
